# 🟦 YASH — Shared Layout (AppShell)

Build the shell EVERY page uses. Do this in Phase 0 so Ajay & Vibhu drop their pages in.

Files:
- AppShell.tsx    (sidebar + topbar + <Outlet/> content area)
- Sidebar.tsx     (logo, role-based nav links, user block at bottom, 240px fixed)
- Topbar.tsx      (page title, search, user menu, dark-mode toggle slot for Vibhu)

Follow DESIGN_SYSTEM.md layout pattern exactly.
