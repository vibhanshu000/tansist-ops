# 🟦 YASH — Styles (set up once, then frozen)

Files:
- globals.css   (Tailwind directives + CSS variables from DESIGN_SYSTEM.md, light + dark)

After Phase 0 this is basically frozen. Vibhu adds the dark-mode variable block when
doing the dark-mode bonus. Nobody adds ad-hoc colors here.
